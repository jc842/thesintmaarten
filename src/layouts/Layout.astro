---
import ReviveSlot from '../components/ads/ReviveSlot.astro';
import MobileStickyAd from '../components/ads/MobileStickyAd.astro';
import Matomo from '../components/analytics/Matomo.astro';
import { siteConfig } from '../config/site.config';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  lang?: string;
}

const {
  title = siteConfig.title,
  description = siteConfig.description,
  image = '/images/og-image.jpg',
  lang = 'fr',
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site || siteConfig.url);
const alternateFr = new URL(Astro.url.pathname.replace(/^\/(en|es)/, ''), siteConfig.url);
const alternateEn = new URL('/en' + Astro.url.pathname.replace(/^\/(en|es)/, ''), siteConfig.url);
const alternateEs = new URL('/es' + Astro.url.pathname.replace(/^\/(en|es)/, ''), siteConfig.url);
---

<!DOCTYPE html>
<html lang={lang} class="scroll-smooth bg-[#08121e] text-slate-100 antialiased selection:bg-rose-500 selection:text-white">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <link rel="alternate" hreflang="fr" href={alternateFr} />
    <link rel="alternate" hreflang="en" href={alternateEn} />
    <link rel="alternate" hreflang="es" href={alternateEs} />
    <link rel="alternate" hreflang="x-default" href={alternateFr} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content={image} />

    <!-- Google Fonts: Space Grotesk -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style is:global>
      body {
        font-family: 'Space Grotesk', sans-serif;
      }
    </style>
    <!-- Matomo Tag Manager (Privacy-First / Cookieless) -->
  <Matomo />
</head>
  <body class="flex min-h-screen flex-col bg-[#08121e] text-slate-200">
    <!-- Header Spaziobianco -->
    <header class="sticky top-0 z-50 border-b border-slate-800/80 bg-[#08121e]/90 backdrop-blur-md">
      <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-3.5 sm:px-6">
        <a href="/" class="flex items-center gap-3">
          <div class="flex h-10 w-10 items-center justify-center rounded bg-gradient-to-tr from-cyan-500 to-blue-600 font-bold text-white shadow-md shadow-cyan-900/30">
            SXM
          </div>
          <div>
            <span class="block text-base font-bold tracking-tight text-white uppercase">The Sint Maarten</span>
            <span class="block text-[10px] font-medium tracking-widest text-cyan-400 uppercase">Dutch Caribbean Guide</span>
          </div>
        </a>

        <!-- Navigation Links -->
        <nav class="hidden md:flex items-center gap-7 text-xs font-medium tracking-wide text-slate-300">
          <a href="/blog/" class="hover:text-cyan-400 transition-colors uppercase">SIMPSON BAY</a>
          <a href="/blog/" class="hover:text-cyan-400 transition-colors uppercase">PHILIPSBURG</a>
          <a href="/blog/" class="hover:text-cyan-400 transition-colors uppercase">CUPECOY & MAHO</a>
          <a href="/mentions-legales/" class="hover:text-cyan-400 transition-colors uppercase">INFOS PRATIQUES</a>
        </nav>

        <!-- Languages & Badges -->
        <div class="flex items-center gap-3">
          <div class="hidden sm:flex items-center gap-2 rounded-full border border-cyan-500/30 bg-cyan-950/40 px-3 py-1 text-[11px] text-cyan-300 font-medium">
            <span class="h-1.5 w-1.5 rounded-full bg-cyan-400"></span>
            DUTCH CARIBBEAN
          </div>
          <div class="flex items-center gap-1.5 text-xs text-slate-400 border-l border-slate-800 pl-3 font-semibold">
            <a href="/" class="px-1.5 py-0.5 rounded text-white bg-slate-800">FR</a>
            <a href="/en/" class="px-1.5 py-0.5 rounded hover:text-white">EN</a>
            <a href="/es/" class="px-1.5 py-0.5 rounded hover:text-white">ES</a>
          </div>
        </div>
      </div>
    </header>

    <!-- Leaderboard Ad Slot -->
    <div class="hidden md:flex justify-center my-4">
      <ReviveSlot slot="header" />
    </div>

    <main class="flex-grow">
      <slot />
    </main>

    <!-- Mobile Sticky Banner -->
    <MobileStickyAd />

    <!-- Footer Spaziobianco -->
    <footer class="border-t border-slate-800 bg-[#060e18] py-12 text-slate-400 text-xs">
      <div class="mx-auto max-w-7xl px-4 sm:px-6">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">
          <div class="md:col-span-2">
            <div class="flex items-center gap-2 mb-3">
              <div class="h-7 w-7 rounded bg-gradient-to-tr from-cyan-500 to-blue-600 font-bold text-white flex items-center justify-center text-xs">SXM</div>
              <span class="text-sm font-bold text-white">THE SINT MAARTEN EDITORIAL</span>
            </div>
            <p class="text-xs text-slate-400 leading-relaxed max-w-md font-light">
              Le guide de référence pour explorer Sint Maarten : superyachts à Simpson Bay, plages emblématiques, boutiques détaxées à Philipsburg et art de vivre des Caraïbes néerlandaises.
            </p>
          </div>
          <div>
            <h4 class="text-slate-200 font-semibold mb-3 tracking-wider text-[11px] uppercase">Navigation</h4>
            <ul class="space-y-2 text-slate-400">
              <li><a href="/" class="hover:text-cyan-400 transition-colors">Accueil</a></li>
              <li><a href="/blog/" class="hover:text-cyan-400 transition-colors">Tous les Guides SXM</a></li>
              <li><a href="/en/" class="hover:text-cyan-400 transition-colors">English Edition</a></li>
              <li><a href="/es/" class="hover:text-cyan-400 transition-colors">Edición en Español</a></li>
            </ul>
          </div>
          <div>
            <h4 class="text-slate-200 font-semibold mb-3 tracking-wider text-[11px] uppercase">Légal & Normes</h4>
            <ul class="space-y-2 text-slate-400">
              <li><a href="/mentions-legales/" class="hover:text-cyan-400 transition-colors">Mentions Légales</a></li>
              <li><a href="/politique-confidentialite/" class="hover:text-cyan-400 transition-colors">Politique de Confidentialité</a></li>
              <li><span class="text-slate-500">100% Cookieless RGPD</span></li>
            </ul>
          </div>
        </div>
        <div class="border-t border-slate-800/80 pt-6 flex flex-col sm:flex-row justify-between items-center text-[11px] text-slate-500">
          <p>© {new Date().getFullYear()} The Sint Maarten — Dutch Caribbean Media Guide.</p>
          <p class="mt-2 sm:mt-0 font-semibold text-cyan-400">LEXINGTON SPAZIOBIANCO ARCHITECTURE</p>
        </div>
      </div>
    </footer>
    <!-- Masquage propre des emplacements Revive sans bannière active -->
  <script is:inline>
    (function() {
      const reviveId = 'ac119b122a644588953c74c4c1daee06';
      document.addEventListener('revive-' + reviveId + '-loaded', function(e) {
        if (e && e.detail && e.detail.data && (!e.detail.data.width || Number(e.detail.data.width) === 0)) {
          const insEl = document.getElementById(e.detail.id);
          if (insEl) {
            const container = insEl.closest('.revive-slot-container') || insEl.parentElement;
            if (container) {
              container.style.display = 'none';
              const parentWrapper = container.parentElement;
              if (parentWrapper && (parentWrapper.classList.contains('flex-col') || parentWrapper.classList.contains('hidden') || parentWrapper.classList.contains('my-10') || parentWrapper.classList.contains('my-14') || parentWrapper.classList.contains('my-12')) && parentWrapper.children.length <= 2) {
                parentWrapper.style.display = 'none';
              }
            }
          }
        }
      });
    })();
  </script>
</body>
</html>
